<!-- 由 ideahub-app/docs/support-knowledge-base.md 剥掉代码出处后生成（scratch/strip_kb.py），生成日期 2026-09-03。改知识请改 app 仓的源文件后重新生成，不要直接改这里。 -->

# 启梦创作 App — 客服知识库（代码事实版，2026-09-03）

> 用途：AI 客服（看板娘数字人）的知识来源与人工客服的速查手册。

---

## 1. 产品概览、导航与页面

### 1.1 产品定位
- App 是 IdeaHub 的安卓端：React 19 + Vite 7 + Tailwind + Capacitor，形态对标抖音/TikTok 的全屏上下滑视频流，**视频由 AI 逐段生成，支持分支互动**
- 应用名「启梦」，包名 `com.ideahub.branchvideo`
- 当前版本 versionName `2.44` / versionCode `56`
- 登录页标语：「启梦 · 有想法，就是梦想启程的第一步」
- 三仓结构：server（Node+Express5+MongoDB，阿里云 ECS）/ client（官网）/ app（APK/AAB），互相只靠 HTTP 契约耦合

### 1.2 底部 Tab（共 5 格，中间是创作 ➕）
- `/` 首页、`/discover` 分区、（中间 ➕ 创作）、`/workshop` 工坊、`/me` 我的
- 底栏刻意**没有第六格**；通知入口在个人页顶栏铃铛

### 1.3 全部路由
| 路由 | 页面 | 说明 |
|---|---|---|
| `/` | FeedPage | 全屏上下滑视频流；`:243` |
| `/discover` | DiscoverPage | 分区 + 搜索（标题/简介/作者/分类）+ 搜人；`:244` |
| `/workshop` | WorkshopPage | 卡片/卡组资产管理（不设硬登录墙）；`:247` |
| `/me` | ProfilePage | 我的主页；`:248` |
| `/user/:userId` | ProfilePage | 别人主页（正路，按 id）；`:260` |
| `/u/:author` | ProfilePage | 老路径（按展示名），必须保留，老链接/评论仍带它；`:261` |
| `/login` | LoginPage | `:263` |
| `/oauth/callback` | OauthCallbackPage | web 回程（原生走深链）；`:265` |
| `/settings` 及 `/settings/{profile,voice,quality,storage,deactivate}` | 设置系 | 全部 `RequireAuth`；`:271-276` |
| `/admin` | AdminPage | 仅管理员可见入口，服务端 `requireRole("admin")` 才是真门；`:282-289` |
| `/notifications` | NotificationsPage | 全屏推入；`:294-301` |
| `/video/:id` | VideoPage | 作品详情（多 P + 分段剧情 + 评论）；`:302` |
| `/card/:id` | CardDetailPage | 卡片详情；`:303` |
| `/custom-card` | CustomCardPage | 自己传图做卡片；`:309-316` |
| `/drafts` | DraftsPage | 草稿箱整页，**不设登录墙**（草稿在本机）；`:318` |
| `/deck/:id` `/template/:id` `/templates` | 卡组/模板详情、模板市场；`:319-321` |
| `/create` | CreatePage | 创作岔路口；`:322-329` |
| `/studio` | StudioPage | 3D 卡片工坊（含全屏画布浮层）；`:330-337` |
| `/simple` | SimpleModePage | 简约模式向导；`:339-346` |
| `/flow` | FlowPage | 工作流画布 / 简约单段 UI；`:347-354` |
| `/cut` | CutPage | 剪辑页；`:355-362` |
| `/video-editor` | VideoEditorPage | 白模化 / 挂卡；`:370-377` |
| `/publish` | PublishPage | 发布页；`:378-385` |
| `/edit/:id` | EditPage | 作品编辑（只改壳）；`:386-393` |
| `*` | 重定向到 `/` | `:394` |

### 1.4 各页职责（页头注释）
- **FeedPage**：每屏一支，进入视口自动播放、离开暂停；带 branchTree 的互动视频在流里播开场段，点「进入互动」跳详情页
- **DiscoverPage**：分区入口 + 搜索 + 排序，选分区不改变界面结构
- **WorkshopPage**：管理自己的卡片与卡组、搜索添加市场卡片、建组增删卡；与 `/studio`（创作现场）分工
- **StudioPage**：3D 画布 + 投影窗 + NPC 底部抽屉；工作流画布是它的一层全屏浮层
- **ProfilePage**：一套实现服务 `/me`、`/user/:id`、`/u/:author`；`/me` 多出 设置/钱包/草稿/卡片/卡组/收藏
- **CreatePage**：创作只剩两条路 — 工坊模式（3D 铸卡桌面，推演三套方案、逐段落地）与简约模式（单节点，一句话出一条短片，**不进草稿库**）
- **FlowPage**：一段的推进是三拍（写要求 → 推演三套方案 → 挑一套 → 才炼视频）
- **SimpleModePage**：三步（选起点 → 说清楚 → 确认出片），恒单段、恒直出、无方案台、不存草稿
- **CutPage**：顶栏（返回/说明/导出分辨率/下一步）+ 预览 + 三页签（剪辑 / 圈选 / 音频）；「下一步」重编码成单条视频进发布页
- **VideoEditorPage**：一页两模式 — 白模化前（框 5~30 秒、裁水印）与套用挂卡（角色位挂人物卡）
- **EditPage**：只改标题/分类/简介/封面/可见性 + 删除整部作品；**不能改内容**
- **CardDetailPage / DeckDetailPage / TemplateDetailPage / TemplateMarketPage / CustomCardPage / DraftsPage / NotificationsPage**：见各文件头 1-25 行

### 1.5 启动行为
- 冷启动先装 IndexedDB 数据层（视频/账号/社交/模板/草稿/弹幕/剪辑稿），装完前显示「正在打开作品库…」
- 除工坊外全局锁竖屏（native manifest 已钉死，Web 层改不动）
- 已登录但本机没有当前版本协议同意记录的用户，开屏弹补签门；「不同意」只退登录态，不拦浏览

---

## 2. 账号与登录

### 2.1 登录方式（由服务端能力决定，前端不自行判地区）
- 可用方式来自 `GET /api/auth/capabilities`，服务端按**出口 IP** 判地区：中国大陆关掉 OAuth（Google 在墙内点了只会转圈）；短信通道没配就不报 `phoneEnabled`
- 探测失败（离线/老服务端）**退到最小集：邮箱 + 密码**
- 三种表单模式：密码登录（用户名/邮箱 + 密码）、邮箱验证码（注册/重置密码）、手机号（**登录即注册**）
- 手机号只收中国大陆号
- 密码最少 6 位
- 验证码 6 位，**10 分钟内有效**，重发冷却 60 秒
- 第三方：Google / GitHub（走系统浏览器 + 深链回来）、QQ（原生 SDK）、微信（原生 SDK）
- **QQ / 微信只能在 App 里用**，浏览器里点会给出真实原因「要在 App 里用（浏览器中没有 QQ/微信客户端可以拉起），先用邮箱或手机号登录」
- QQ 不在 `providers[]` 里是有意的：QQ 互联注册的是移动应用，后台没有回调地址栏
- 大陆网络下会显示「当前网络环境（中国大陆）下 Google / GitHub 不可用，已自动隐藏」
- QQ 用户没有邮箱，建号时用合成邮箱 `qq_<openid>@no-email.ideahub.local`，用户名随机 `user_<8位hex>`
- 未登录也能逛首页（登录页有「先随便逛逛」）

### 2.2 协议勾选
- 登录前必须勾选《用户协议》《隐私政策》；没勾就点登录会弹「同意并继续」确认卡
- 协议版本以「更新日期」标记，当前 `2026-08-28`；正文更新后需重新同意

### 2.3 密码重置
- 登录页「忘记密码？」→ 切到「邮箱验证码 → 重置密码」；流程：`POST /api/auth/email/reset/start` 发码 → `/reset/verify` 带新密码 → 自动登录
- **手机号账号没有密码重置路径**（手机号是登录即注册） `[待确认]` — 代码里 reset 只有邮箱一条

### 2.4 UID（公开数字号）
- UID = 服务端发的**随机 9 位数字**，与注册顺序/注册时间无关
- 位置：**个人页 `@用户名` 下方那一行**，点按即复制
- 用途：在搜索框里输入这串数字能直接命中这个人
- **离线账号 / 老服务端没有 UID，那一行整行不显示**
- 版本说明口径：「UID 是随机分配的数字，和注册时间无关，别人无法从中推断你的任何信息」

### 2.5 昵称 / 简介 / 头像
- 入口：设置 → 编辑资料（`/settings/profile`）
- 昵称上限 **16 字**，简介上限 **120 字**（输入框 maxLength 与落库 slice 同一个数）
- 头像两条路：AvatarPicker（相册图片，压成 256px；远端模式转存 Cloudinary）与 12 个 emoji
- **保存失败会回滚并明说**：「没能同步到服务器（…）——改动已撤回，联网后再试一次。」远端模式下本机不落盘，PUT 是唯一真相
- 改昵称的**已知历史坑**：缓存里作品的 author 是旧显示名，改名后需按 `authorId` 改写缓存；不然首页右侧头像退回字母底、点进去还是旧名字，重启才好
- `username`（登录名 / @ 句柄）**注册时定下、之后不可改**
- @ 提及：打的是 `username`，屏幕上显示的是对方**当前**的 displayName；对方改名后已发出的 @ 也跟着变

### 2.6 注销账号（SettingsDeactivatePage）
- 入口：设置页「退出登录」下方的小字链接「注销账号」，**仅远端模式显示**
- 确认方式：**原样输入用户名，严格全等**（不 trim、区分大小写），客户端不预先加工
- 页面明示后果三条
  1. 立即生效：所有设备退出登录，这个账号无法再使用
  2. 账号里的 token 余额、已购内容与已发布作品都将一并无法使用
  3. **数据不会立刻从服务器抹除**（防误操作）；恢复账号或要求彻底删除数据发邮件到 `support@ideahubs.org`
- 服务端语义：**软删除** — 只打 `deactivatedAt` + `tokenVersion += 1`（所有旧 token 立即失效），**不级联删除任何内容数据**，管理员可把 `deactivatedAt` 置回 null 恢复
- 注销后**登录也被拦**，401 整句「账号已注销……support@ 可恢复」；错误密码仍报通用凭据错误
- 离线包（本地账号）没有可注销的云端账号，页面如实解释：清 App 数据或卸载即等同注销

### 2.7 退出登录
- 远端模式：「作品、卡片和钱包都记在服务器上，重新登录同一账号就回来。」
- 离线模式：「本机的作品与数据不会被删除，重新登录即可继续。」
-

### 2.8 登录态三态与「我被登出了」
- `authState()` 三态 `in / out / pending`；只有 `"out"` 才把人送去登录页，`pending` 显示加载态
- 冷启动后立刻点底栏 ➕ 曾误弹登录页（2026-08-20 修）；自愈五轮退避跑完会如实退回 `out`，不会永远转圈
- 被服务端终止登录态（**目前只有封禁**）时，登录页开屏会显示原因（`consumeAuthNotice`）

### 2.9 封禁 / 黑名单 / 私信
- **App 里没有黑名单、拉黑、私信、好友申请功能**（全 app grep 无实现；私信/好友申请属于官网产品线）
- 封禁由管理后台执行：「封禁挡的是**登录与一切带登录态的请求**，对方会看到下面这句原因」
- 管理员行不渲染封禁/删除（防手滑），服务端也会拒

---

## 3. 创作流程、草稿与限制

### 3.1 流程
- 底栏 ➕ → `/create` 岔路口 → **工坊模式（`/studio`）** 或 **简约模式（`/simple`）**
- 一段的推进是三拍：写要求 → 推演三套方案（各带首尾帧预览）→ 挑定一套（可换帧、改剧情、按修改重画）→ 才炼视频
- **必须炼出本段视频才能开下一段**（段间靠上一段的真实尾帧承接起拍）
- 出片完成 → 组稿 → 剪辑页 `/cut` → 「下一步」重编码 → 发布页 `/publish`
- 工坊与工作流是**同一条流水线的两个面**，换个模式打开不会要求重炼、不会重复收费
- 生成期间**可以离开工作流页去逛**（站内切页不会中断），结束时会有全局悬浮胶囊叫你回来

### 3.2 草稿
- 草稿 = **还没发布的半成品**，存在**这台设备的 IndexedDB**（`data/drafts.ts`），与账号无关；因此 `/drafts` 不设登录墙
- 草稿索引与正文分开存（正文带 1MB 级的帧）
- **草稿上限 20 条**，超过从最旧的开始清（按 updatedAt）
- 草稿标题重命名上限 40 字
- **简约模式不进草稿库**（只有一段、写一句话就出片、直通发布）
- 自动存盘：每炼出一段自动存草稿（`hooks/useFlowActions`，工坊与工作流共用）
- **卸载 / 换设备 = 草稿全丢**（草稿只在本机 IndexedDB，从不上传） — 由  与  共同推出
- 已发布作品**不可回炉**：成片定稿，编辑页只改壳

### 3.3 换流水线 / 丢段的守卫
- 「整表换掉 nodes」的七条入口都要先问 `flowDirty`，弹 `DiscardFlowDialog` 说清哪些其实存住了
- **在途生成时一律不许换/不许删段**：出片是几分钟的异步、没有 AbortController，换掉后回包时钱照扣、写回落空
- 「有一段正在生成中，等它跑完再换走向」
- 「这一段已经出片：换模板会作废这段成片。想换就删除本段重加，或先接受重炼这一段的花费」

### 3.4 时长与数量限制
- 视频段时长：产品上限 **10 秒**，下限跟档位走（`VideoTier.minSec`；电影级最低 4 秒、真人档只有 6s/10s 两档）
- 白模模板：输入窗口 [5,30]s，模板视频窗口 [4,30]s（两个数不是一回事）
- 服务端 publish schema：segments 1~60 段、标题 1~120 字、简介 ≤4000 字、分类 ≤40 字、tags ≤20 个 × ≤40 字、卡组 ≤60 张
- 作品话题标签（App 侧产品口径，故意小于服务端）：**最多 6 个，每个 ≤10 字**
- 卡名 ≤120 字、卡简介 ≤2000 字（**服务端超了是整发 400，不是截断**）
- 分享推荐语 ≤200 字
- 图位花名 ≤24 字
- 视频提示词上限 400 字
- 一次铸卡最多出几张卡：卡组 `DECK_MAX_CARDS = 8`、模板 `TEMPLATE_MAX_CARDS = 6`
- 一个卡组最多 2 个 3D 模型（`DECK_MAX_3D = 2`）
- 一张卡最多 2 张图进出片管线（第 3 张画了也用不上，档位 `views` 不许写 3）
- 参考图协议上限：2.0 系列 1–9 张，2.5 系列 1–30 张

### 3.5 上传队列（发布失败的成片）
- 发布失败的作品进「待上传队列」，每条**记着主人**，只发自己的（换账号后别人那条原样留着）
- 队列项是那条付费成片在磁盘上的**唯一指针**，删了就再也传不上去

---

## 4. AI 生成与费用

### 4.1 供应商与模型
- 主供应商：**火山引擎方舟（Ark v3）** — Seedream 生图 / Seedance 生视频 / 豆包对话
- 真人档换供应商：**MiniMax 海螺 2.3（768P）**，境内 `api.minimaxi.com`
- TTS 走火山 openspeech（另一条产品线，另配 `TTS_APPID`/`TTS_TOKEN`）；**方舟没有 TTS**
- 密钥永远在服务端，不进 App 包

### 4.2 视频档位（VIDEO_TIERS）
| id | 名称 | 模型 | 系数 | 首尾帧 | 参考图 | 白模 r2v | 环境音 | 真人照片 | 最短 | 门槛 |
|---|---|---|---|---|---|---|---|---|---|---|
| fast | 极速 | seedance-1-0-pro-fast | 4.2/15 | ✗ | ✗ | ✗ | ✗ | ✗ | 3s | — |
| std | **标准（默认）** | seedance-1-0-pro | 1 | ✓ | ✗ | ✗ | ✗ | ✗ | 3s | — |
| hd | 高清 | seedance-2-0-mini | 23/15 | ✓ | ✓(≤9) | ✗ | **✓** | ✗ | 3s | — |
| ultra | 电影级 | seedance-2-5 | 4.7 | ✓ | ✓(≤30) | ✓ | ✓ | ✗ | **4s** | **仅付费套餐** |
| real | 真人 | MiniMax-Hailuo-2.3 | 按发定价 | ✗ | ✗ | ✗ | ✗ | **✓** | 6s | — |

- 默认档 `DEFAULT_TIER = "std"`
- 「高清」档**免费套餐就能选**，切过去就有 AI 生成的环境音；开音频零额外成本
- 真人档只有 6s / 10s 两个整档（海螺 768P 的全部合法时长），按发一口价 **135,000 / 270,000 token**
- 真人档**没有「推演三套方案」这一步**（按发直出）；提示句见 `deriveIssue`
- **除真人档外，四档全部拒收真人照片**（方舟两套探测器：名人按版权拦、普通人按隐私拦，整发拒收）
- 真人卡的合规出路：做**肖像授权**拿到方舟可信素材 ID（`asset://`），高清/电影级两档收它
- 真人卡 × 白模模板是**双向都无解**的组合

### 4.3 出图档位（IMAGE_TIERS）
| id | 名称 | 模型 | 张数 | 说明 |
|---|---|---|---|---|
| **sketch（默认）** | 速写 | seedream-4-0 | 1 | 只画 1 张主图，它同时就是卡面 |
| studio | 定妆 | seedream-4-5 | 2 | 主图 + 一张参考图 |
| master | 精绘 | seedream-5-0-pro | 2 | 换旗舰模型，形象更准、**出图较慢** |

- `DEFAULT_IMAGE_TIER = "sketch"`
- 出图耗时实测：4.0 ≈11.6s、4.5 ≈7.1s（比 4.0 还快）、5.0-pro ≈95.7s@1728×2304
- 精绘档一张卡三张图串行 ≈ 将近 5 分钟，客户端出图超时 170s

### 4.4 钱包、套餐与额度
- 钱包两笔账：`plan`（当月套餐额度，**跨月刷新、未用完作废**）、`addon`（直充与退款，**永不过期**）；扣减**先 plan 后 addon**
- 套餐（`PLANS`）
  - 免费版 ¥0/月，**300,000 token**，注册即得、每月刷新
  - 标准套餐 ¥30/月，2,000,000 token（约 15 段标准档）
  - 专业套餐 ¥98/月，8,000,000 token（约 60 段标准档）
- 直充包（进 addon，永不过期）
- 钱包入口在「我的」页 → 「充值 / 套餐」抽屉
- **「电影级」仅付费套餐**；免费版看得见但点不动，并写出原因：「「电影级」单段消耗超过免费版整月额度（300.0k），升级套餐后可用」
- 管理员账号 **AI 生成免扣费**，界面上会如实写出来
- 余额不足时的整句：「推演一次约 X token，余额 Y 不足——去「我的」页充值」
- 服务端余额不足 → **402 `INSUFFICIENT_TOKENS`**，方舟根本不会被调用

### 4.5 计费与退款（**这是客服最关键的一节**）
- 契约：**先扣钱、再转发；上游没受理就原路退回**
- 上游非 2xx（400 敏感词 / 429 限流 / 5xx / 501 没配 key）→ 扣掉的**原路退回 addon**（不退 plan，因为 plan 跨月作废）
- ⚠ **任务被受理之后才失败（Seedance 排队跑完报 failed）不退**——那时算力已消耗、方舟也已向我们计费。**刻意为之，不是遗漏**
- 查询任务状态 `GET .../tasks/:id` 与取产物 `GET /asset` 计费为 **0**
- 每个响应带 `X-Wallet-Plan` / `X-Wallet-Addon` 权威余额头，App 钱包镜像据此同步
- 客户端钱包是**镜像不是账本**；远端模式下 `spendTokens` 是空操作，真扣款在服务端
- 报价与实扣共用同一函数（`segTokens` 等），两仓价目表必须逐条相等，服务端 spec 钉住

### 4.6 「取回」= 避免二次付费（客服高频）
- 出片是先扣钱再等，最长等 **25.5 分钟**（死线按输出秒数缩放：`max(12分钟, 输出秒×90s + 3分钟)`）
- 轮询节奏：前两分钟 5 秒一次，之后 10 秒一次
- 到点不算失败，抛 `ArkTaskUnknown`：「等了 N 分钟还没出片。这不是失败：任务还在方舟那边跑，钱在提交那一刻就已经花掉了。」
- 连查 5 次失败：「盯不住这一发的进度了（…）。任务还在方舟那边跑，不是失败：钱在提交那一刻就已经花掉了。」
- **凭据存本机 localStorage，有效期 24 小时**（方舟产物 TOS 签名地址 24h 过期）
- 未过期提示语（方舟档）：「还剩 X 小时 Y 分钟可以取回——方舟的成片只留 24 小时，过期就没了。这一发的钱在提交那一刻就已经花掉了，**取回不再花一分钱**；点「重新生成」是重新下一单、会再花一次。」
- 过期提示语（方舟档）：「这一发已经取不回来了：方舟的成片只在服务器上留 24 小时，现在已经过期。**已经花掉的钱无法挽回**——这不是超时重来，重新生成是再花一次钱。」
- 真人档（MiniMax）**不许说 24 小时**（我们没量过 MiniMax 留存），过期文案是「这一发我们不再跟进了：任务号还在下面，如果一直没取回来，**把它发给客服还有机会**。已经花掉的钱我们这边退不了——重新生成是再花一次钱。」
- 真人档未过期：「出片通常 1~2 分钟，隔一会儿再点一次。」
- 真人档上游明说失败：「上游说这一发失败了：…。按约定，**受理之后的失败不退款**；重新生成会再花一次钱。」
- 取回成功会补扣一次 token（保证一段视频只扣一次，不是零次）；远端模式下真扣发生在提交那刻，取回一分不多花
- **只有已过期的凭据才允许「知道了」消掉**（没过期的里面躺着还能取回来的钱）

### 4.7 「圈选白花钱」的防护
- 承接段（第 2 段起）的开头画面由上一段的**真实尾帧**顶替，圈在前半段的修改画出来会被覆盖 → 现在**不跑也不收钱**，并且在按下重炼**之前**就说明
- 页面整句：「另有 N 处落在承接段的前半段（开头画面用的是上一段的真实结尾，不重画）——**不计费**，但你写的要求仍会随出片提示词发出去。」
- 带参考视频的段整句：「这一段带着参考视频，出片时不接受圈选改画面——这 N 处得先清掉才能开炼（它们也不计费）。」
- 剪辑页：「另有 N 处落在已删掉的段上，不计费也不会重拍」
- 提取器：选段外的标记「**不会被采用、也不计费**」
- 铸卡：「少画的那张不会去调出图，也就不计费」
- 价目缺失时**既不报价也不开炼**（`imageTierPriceIssue` / `r2vPriceIssue`），绝不静默按 0 收或静默降级
- 确认卡执行前用同一把尺重算价钱与顺序门禁，对不上整句拒（防「标价 507.6k、实扣 1.0M」）
- 出图失败退回原图的那张**不收图钱**
- 自定义卡片页（`/custom-card`）**一个模型都不调，既不报价也不扣 token**

### 4.8 生成失败的典型原因与文案
- **提示词含敏感词 → 整个请求 400（`InputTextSensitiveContentDetected`），不是降级而是失败**；「少女」「拥抱」「害羞」等词会被拒
- 一张图里画多个角色**一律被拒**
- 「推演失败：…」/「重画失败：…」（截 120 字）
- 演示模式（未连服务端 / 未配 key）：AI 全走 mock，成片是首尾帧渐变。发布页会给每段打标：「✓ 真实影像」或「⚠ 渐变回退」
- 出片成功后自动转存 Cloudinary 换永久地址；转存失败退回方舟直链（24h 有效）并说明「跨境网络下预览可能很慢」
- 段与段的承接：高清/电影级两档为了能同时发素材卡形象图，起拍是「参考图 + 提示词点名」而不是协议级硬约束 → **不能说「无缝」「严丝合缝」**
- 圈选改帧是**软引导**，不能说「一定按你圈的改」

---

## 5. 语音 / TTS

### 5.1 SettingsVoicePage（`/settings/voice`，标题「铸卡师的声音」）
- 三块：音色列表（点即试听并选中）、语速、语调指令
- 试听台词固定：「欢迎来到卡片工坊，把你的素材交给我，我为你炼成卡片。」
- 语速滑杆：−30 ~ +20，步长 5，标尺 0.70×慢 / 1.00× / 1.20×快；可「恢复跟随音色」
- 语调指令：自由文本，**上限 120 字**，可「恢复默认」；这个旋钮比换音色更管用
- 「调和」标签的混音音色**语调指令对它无效**（那是 2.0 专属）

### 5.2 音色清单
- 默认：**清冷高雅 2.0**（rate −10，支持表现力增强）
- 5 个混音配方（御姐+知性+女友 / 古风少御+御姐 / 古风少御+知性+柔美 / 御姐+温柔淑女+小雅 / 俏皮+御姐+女友），统一 rate −20
- 单音色 2.0（11 个）：高冷御姐、知性女声、知性灿灿、魅力苏菲、温柔淑女、清新女声、小何、古风少御、Vivi、顾姐、魅力女友、TVB女声
- ICL 角色扮演档（6 个，支持表现力增强）：成熟姐姐、成熟温柔、理性圆子、邪魅女王、温柔女神、病娇姐姐
- 三条选型硬约束：只收 2.0 音色（1.0 未开通）、**绝不收 IP 仿音**（法律风险）、一律中文音色

### 5.3 云端 vs 系统合成器降级
- 试听打 `POST {API_BASE}/api/tts`；返回 404/501/401/403 时**真的退回系统内置合成器**
- 页面常驻一句：「没配云端语音时退回系统内置合成器（需装中文语音包）。」
- 两句错误文案：
  - 「这台设备既没有云端语音，系统也没装中文语音包，试听不了」
  - 「云端语音没出声（状态码）：稍后再试，或到火山控制台看额度」
- **「铸卡师不出声（嘴在动没声音）」= 系统没装中文语音包**；Windows 上装完要完全退出浏览器再开（语音表在进程启动时枚举一次）
- `/api/tts` 必须走 `API_BASE`，不能同源相对路径（Capacitor SPA 回退返回 200+HTML）
- 服务端 TTS：`POST /api/tts` 必须登录，按用户限流 **30 次/分钟**，text ≤300 字（超出截断）

### 5.4 人物卡的声音样本（跟读录音）
- 长按按钮照着示例句念，**2~15 秒**（Seedance 参考音频硬窗口），到 15s 自动停
- 太短提示：「太短了（X.Xs）——按住别松，照着示例词念够 2 秒再放手。」
- 麦克风权限失败提示：「App 版本 ≤2.29 的安装包没带录音权限，升级到新版后在系统弹窗里点允许；拒绝过的话去系统设置 → 应用 → 启梦 → 权限里打开麦克风。」
- 生效条件：出片走「高清/电影级」档、台词写在引号里
- **声音样本只存本机 IndexedDB（侧库），不跨设备、不随分享**
- 工作流的首尾帧承接段**带不了音色参考**（协议互斥，不是漏做）
- 音色点名句放不下时会明说「放不下就不发，并告诉你把要求写短些就能带上」

---

## 6. 发布与社交

### 6.1 发布页（`/publish`）
- 可填：标题（必填）、分类、简介、封面、话题标签、可见性、「随片带上这套卡组」开关
- 分类固定 6 项：剧情 / 科幻 / 古风 / 搞笑 / 动画 / 其他
- 可见性**默认公开**
- 「随片带上这套卡组」**默认开**（关掉别人只看得到成片）
- 「付费解锁」**已于 2026-08-31 下架**，页面写着：「目前所有作品都是免费观看。付费解锁还没开放——等收款和分账真的通了再放出来，免得你以为在收钱、其实一分也到不了账。」
- **发布即定稿，不可回炉**

### 6.2 可见性三档
| 界面档位 | 列表/搜索 | 按链接直接打开 |
|---|---|---|
| 公开 | 出现 | 放行 |
| 凭链接可见 | **不出现** | **放行** |
| 仅自己可见 | 不出现 | 仅作者 |
— ；
- 「凭链接可见」的代价在分享面板上明说：「拿到链接的人都能看，也能转给别人」
- 从「凭链接可见」改成「仅自己可见」必须显式下发 `linkOnly: false`

### 6.3 分享
- 三个入口：QQ 好友（原生卡片分享）、微信好友（原生）、复制链接
- **分享链接是官网预览页 `https://ideahubs.org/v/<id>`**
- 三种情况下三颗分享键**全部禁用并说明原因**
  - 还在上传中/没连上服务器：「现在分享出去的链接别人打不开。等它传完再分享。」
  - 已被平台下架：「这条已被平台下架，链接别人打不开，所以先不能分享。原因：…」
  - 私密作品：「链接只有你自己打得开。想给别人看，去编辑里改成「凭链接可见」或「公开」。」
- 官网预览页看不到的兜底文案：「这条视频不存在或已下架。去启梦 App 里看看更多作品吧。」

### 6.4 互动与防刷
- **播放计数**：要真看够 **3 秒**（`PLAY_MIN_SEC`，按 currentTime 增量累计）且一次会话只记一次
- **点赞**幂等；离线模式把 likedIds 落盘
- **收藏跟着账号走**（v2.41 起），此前只活在内存里、杀掉 App 就没
- 评论正文服务端上限 **1000 字**
- 弹幕上限 **40 字**，走服务端，**不做乐观插入**（真等回包）

### 6.5 通知
- **通知只有远端模式有**；离线模式如实说明，绝不编假通知
- App 只显示 6 类（`BRANCH_NOTIFICATION_TYPES`）

| type | 界面文案 |
|---|---|
| BRANCH_LIKE | 赞了你的作品 |
| BRANCH_COMMENT | 评论了你的作品 |
| BRANCH_COMMENT_REPLY | 回复了你的评论 |
| BRANCH_COMMENT_LIKE | 赞了你的评论 |
| BRANCH_MENTION | 在评论里 @ 了你 |
| ADMIN_NOTICE | 平台通知（管理员发的自由文本，**不显示是哪个管理员**） |
— ；服务端 enum

- 一次拉 50 条，**不做无限翻页**
- 通知去重：同一个人对同一条作品的同一件事，**24 小时内只提醒一次**
- 「全部已读」不会误伤官网那边的私信/好友申请
- 老服务端不支持通知时，页面说「这台服务器还没有这个功能」而不是空列表
- 四种状态都要画出来：加载中 / 出错 / 空 / 离线

### 6.6 举报与下架
- 举报入口在**作品 / 评论 / 弹幕**三处，离线模式整块不渲染
- 6 个理由：色情低俗 / 血腥暴力 / 人身攻击·辱骂 / 垃圾营销·刷屏 / 侵权·冒用他人作品 / 其他
- 补充说明选填，≤100 字
- 成功回执：「已收到，管理员会尽快处理。」
- **同一个人对同一个对象只能举报一次**（服务端 409，UI 如实说「你已经举报过了」）
- 举报限流：10 次/分钟 + 50 次/24 小时（按账号）
- 自己的内容不显示举报按钮（该删不该举报）
- 管理员三种处置：**下架（takedown）/ 驳回（dismiss）/ 删除（delete）**
- **下架只对别人生效**：作者自己在首页看那条片子与平常毫无区别，但必须看得见下架状态与原因
- 下架与可见性是两个互不顶替的开关：作者把 visibility 改回 public 也没用

---

## 7. 设置页

`/settings` 只是入口列表，每个功能一页（2026-08-27 拆分）

### 分组一：个性化
1. 🪪 **编辑资料**（`/settings/profile`）— 头像·昵称·简介
2. 🎙️ **铸卡师的声音**（`/settings/voice`）— 行上显示当前音色名
3. 🎨 **画面质量**（`/settings/quality`）— 行上显示当前档 + 「只影响 3D 工坊」

### 分组二：本机
4. 🧹 **本机缓存 / 存储**（`/settings/storage`）— 「查看用量 · 清理中间文件」；标题按模式变（远端=本机缓存，离线=存储）
5. 💡 **新手引导** — 弹确认窗，「恢复」= 把所有引导重置成没看过；**只清这台设备**（引导状态存 localStorage，不上服务端）
6. 📦 **版本与更新** — 显示 `版本 X.XX（versionCode）`

### 分组三：协议与须知
7. 📜 **用户协议**、🔒 **隐私政策**、🏷️ **AIGC 内容须知**，各带更新日期 `2026-08-28`

### 分组四（仅管理员可见）
8. 🛡️ **管理后台** + 一句「你的账号是管理员：AI 生成走的是免扣费通道」

### 底部
- **退出登录**（整宽按钮，带确认）
- **注销账号**（小字链接，仅远端模式）

### 7.1 画面质量三档（只影响 3D 工坊，不影响出片）
- 流畅：1K 贴图 · 4 万面 · 加载最快（她 0.9MB／他 0.8MB）· 老手机选它
- 均衡：2K 贴图 · 8 万面（推荐；她 1.9MB／他 1.8MB）
- 极致：4K 贴图 · 15 万面 · 最清楚但吃显存（她 3.9MB／他 3.8MB）
—
- **首次进工坊会自动定档**，设置页是唯一修改入口；切换会 `location.reload()` 整页重载

### 7.2 更新机制（关键区分）
- **两个渠道**：`sideload`（自己发的直装包，**有自更新**）与 `play`（上架包，**没有自更新**，拿到的是会 reject 的空壳）
- Google Play 政策禁止应用自己下载安装 APK
- 侧载包：设置页版本行显示「可以检查有没有新版本」+「检查更新」按钮；上架包显示「由应用商店负责更新」，**不显示按钮**
- 启动时**延后 3 秒**自动查一次，查不到一律安静收场；手动检查失败会把原因说出来（区分「已经是最新版本」和「检查失败」）
- 用户点过「以后再说」的版本不再打扰，出了更新的才再提
- 更新走原生 HTTP（不走 WebView fetch，避免跨域）；下载后按 sha256 校验整包
- v2.43 起**下载更新支持断点续传**，最多重试 5 次
- 已知坑：**用同一个版本号发第二份内容 → 已下载过旧包的用户永久更新不了这一版**（CDN `max-age=1y, immutable`），屏幕上那句「校验不通过（可能没下完或被中间人改过）」指向的是不存在的原因；出路是涨版本号重出包
- 已知坑：**出包忘涨 versionCode → 已装用户永远收不到更新**（更新检查靠这个整数判新旧）

### 7.3 下载页
- **官网下载页：`https://ideahubs.org/download`**
- 页面同时提供两个 App：**启梦**、**诗绘**
- **iOS 提示：「iOS 版还没有，这是安卓安装包。」**
- **微信/QQ 内置浏览器下不了安装包**：「请点右上角「···」选择「在浏览器打开」，再回到这一页下载。」
- 安装三步：手机浏览器点按钮下载 → 系统提示「禁止安装未知应用」时按提示开启「允许安装未知应用」→ 通知栏/文件管理点开安装包
- **装到最后提示「应用未安装」** → 多半是手机上还留着早期签名不同的测试版，**先卸载再装**
- 页面提供 sha256 校验值供核对
- 数据来自服务端 `/api/app/latest.json`（启梦）与 `/api/app/<id>/latest.json`（其它 App）

---

## 8. 存储与离线

### 8.1 SettingsStoragePage（`/settings/storage`）
- 显示「已用 X MB / 可用约 Y MB」+ 进度条（来自 `navigator.storage.estimate()`）
- 说明按模式分两句
  - 远端模式：「作品与卡片已同步到服务器，换设备登录同一账号即可看到；这里是它们在本机的副本，加上生成过程中的中间文件。」
  - 离线模式：「作品与卡片存在本机数据库里。AI 生成的画面体积较大，空间不足时请删除旧作品。」
- 「清理缓存（可释放 X MB）」按钮 + 确认窗；确认窗承诺：「**只删生成过程中留下的、已经没人用的中间文件。未发布的草稿和还没传上去的作品不会动。**」
- 没得清时明说「没有可清理的中间文件」，不做假忙

### 8.2 清理的真实边界
- **只清两类键**：`merged:<id>`（剪辑合并出的成片，几十 MB）与 `model3d:<id>`（AI 生成的 3D GLB，36MB 级）
- **只扫 24 小时以前的键**（刚合出来还没保存的成片不碰）
- 永不碰：未发布草稿（正文+索引）、待上传队列、账号库、互动数据、弹幕、设置
- **引用收集不完整时一个都不删**
- 历史事故（v2.42 修）：「清理缓存」可能删掉还没传上去的成片，触发条件是冷启动没连上服务器、或这台手机上换过账号

### 8.3 存储满 / 写入失败
- IndexedDB 写失败（配额满或隐私模式）返回 `false`，**调用方必须报给用户，不能静默当成保存成功**
- 待取回凭据存 localStorage，配额满时内存那份还在（这次会话仍取得回，重启才丢）

### 8.4 离线 / 本地模式
- 开关是 `VITE_API_BASE`：有值=远端模式，空=离线模式（走 IndexedDB）
- **配了地址 ≠ 跑在远端**：服务器打不通时整体回退 IndexedDB，照常能登录、炼卡、看种子作品
- 登录页底部按模式给不同承诺
  - 远端：「已连接服务器：账号与作品跨设备同步。」
  - 离线：「当前为本地账号：数据存在这台设备上，换设备不同步。」
- 离线模式下：通知/举报/注销/充值全部不可用或整块不渲染
- 「配了服务端但这次没连上」时充值会拒绝并说「当前未连接服务器，暂时无法充值，请联网后重试」（不会做假余额）

---

## 9. 已知问题与近期变更（用户可见）

### 9.1 环境性/平台性坑（用户会遇到）
- **Capacitor 的本地静态服务器对未命中路径做 SPA 回退，返回 200 + index.html 而不是 404** → 若接口写成同源相对路径，症状是「出片第 1 段就失败：`Unexpected token '<',"<!doctype"...`」、工坊 NPC 不回话、试听没声音。已收口到 `API_BASE`
- **铸卡师不出声**（嘴在动没声音）= 系统没装中文语音包
- **把 debug 包发给别人装** → 下次发 release 包对方装不上，只提示「应用未安装」
- **换过模型资源却没顶版本号** → 老用户的 WebView 拿的还是缓存里那份旧模型，零报错
- **冷启动立刻点 ➕ 被弹去登录页**（2026-08-20 已修）
- **改完昵称后作品列表还挂着旧名字**（已按 authorId 修）

### 9.2 v2.44（当前版）
> 注：仓库根的 `RELEASE_NOTES.md` 当前内容是「修看不见的亏」那一版（圈选不白花钱、自定义参考视频带声音样本、音色点名句放不下时明说、承接段不再一律写「无缝」）

**v2.44**（真人卡授权）：
- 授权成功后不再要求重新传图；「重选方案」不再静默丢掉真人授权；「取消」授权会连照片一起撤；工坊「推演三套方案」补上真人卡门禁（扣钱之前就拦）；授权过的真人卡画帧时形象不再丢

**v2.43**：
- 白模视频切帧屏加了播放/暂停、0.25× 慢放、±0.1s 微调键；切点粒度 0.5s → **0.1s**（「AI 看哪几帧」仍是 0.5s，服务端定死）
- 真人素材授权后照片自动接进来（此前误报「没取到授权照片」）
- 授权完可直接选方案（三套：无面部白模三视图 / 全身立绘+面部特写 / 角色设定规格图）
- **下载更新支持断点续传**（最多重试 5 次）
- 服务端侧：安装包下载快了约 10 倍（命中边缘缓存）

**v2.42**（全是修复，三条是 v2.40 带出来的）：
- 简约模式出片后不再把你扔进 3D 工坊
- 真人档「取回」按钮此前根本不出现，现在真的会出现
- 收藏页没网时不再谎称「有 N 条被作者删了」
- 「清理缓存」不再可能删掉还没传上去的成片
- 网络不好时打开工坊不再把卡库显示成空
- 作品被下架后分享面板会禁用并说明原因
- 评论抽屉刚打开就发评论不再「出现一下然后消失」
- 「凭链接可见」作品里 @ 别人对方收不到通知（服务端已修）

**v2.41**：
- 「收入我的卡组」以前**什么都没存**，现在真的存住了
- **收藏跟着账号走**（此前只活在内存里，杀 App 就没）；本机已有收藏下次登录自动并上去
- 真人档也能「取回」了
- 隐私：声明过「真实人物」的卡，形象参考图**不再随作品发给别人**
- 发布页多了「随片带上这套卡组」开关（默认开）

**v2.40**：
- 出片没接到结果时，工作流画布与工坊里也有「取回」了（此前只在简约模式有）
- 自定义参考视频出片没接到结果时以前连凭据都不留
- 真人档出片过程中网络抖动（5G↔WiFi 切换）不再整发判失败
- 同一台手机换账号后，上一个人没传上去的作品不再被发成新登录者的作品
- 编辑资料同步失败不再谎称「已保存 ✓」
- 收藏重启后消失（已修，⚠ 当时只存本设备）
- 从通知/分享链接进详情页时点赞态显示错误
- 装来的副本不能再分享一遍（防顶替市场原作者）

**v2.39**（全是「屏幕上说的和实际做的不一样」）：
- 个人页作品墙改成直接问服务器（此前会漏，尤其「仅自己可见」的）
- 作品编辑页「保存」失败不再谎称成功（含「改成仅自己可见」）
- 剪辑页分割后「还原整段」会重叠、「取消」合并停不干净、后台切换卡死等
- 工坊「推演三套方案」从第 2 段起按 6 张图收费实际只画 3 张（已改按实际张数收）
- **发布页的「付费解锁」暂时收起**（收款和分账还没做完）

**v2.38 / v2.37 / v2.36 / v2.35**（同一份合流说明）：
- 新增微信登录、微信好友分享
- **个人页新增 UID**（9 位随机数，点一下复制，搜索框输入可直达）
- 「自己传图做卡片」的人物卡拆成四步
- 真人卡「跟读录音」（2~15 秒）
- 出片新增「自定义」参考视频路
- 成片可以圈着改（圈选）
- 工坊与工作流成为同一条流水线的两个面
- **设置页重做成每功能一页**；说明搬进引导弹窗
- 新增《用户协议》《隐私政策》《AIGC 内容须知》
- **新增应用内注销账号**
- 安装包体积：三档模型 42.9MB → 6.3MB

**v2.33**：设置页重做、引导弹窗、三份协议、应用内注销、危险动作加确认窗

---

## 10. 政策、联系方式与内容标识

### 10.1 运营主体与联系方式
- **运营者：刘天彬（个人开发者）**
- **联系/客服/申诉邮箱：`support@ideahubs.org`**（真实存在的 Zoho 收件箱）
- 页脚只挂在官网 `/download` 与 `/privacy` 两页（给站外/审核方看）

### 10.2 隐私政策位置
- **官网：`https://ideahubs.org/privacy`**（不登录可访问）
- **App 内：设置 → 隐私政策**（弹窗全文）
- 官网版最近更新：**2026 年 8 月 24 日**；App 版更新日期 **2026-08-28**
- ⚠ 两份文本不是同一份，日期与措辞不同（App 内是工程侧起草的初稿）

### 10.3 隐私要点（官网口径，可直接答用户）
- **服务器位于中国香港（阿里云）**，数据库由 MongoDB Atlas 托管；在中国内地使用即信息会被传输到内地以外处理
- 第三方清单：火山方舟（提示词/参考图/首尾帧）、MiniMax & Runway（仅真人视频档）、Cloudinary（图片视频）、Resend（邮箱验证码）、阿里云号码认证（短信）、QQ互联/微信开放平台/Google/GitHub（第三方登录）、MongoDB Atlas
- 密码 bcrypt 哈希保存，**我们看不到明文，也无法帮你找回原密码**
- IP 不以明文保存，只用于三件事：判国家（决定显示哪几种登录方式）、限流计数、播放去重时加盐哈希
- 账号信息保留到注销为止；注销后账号被标记为停用，所有登录凭证立即失效
- 导出或彻底删除数据 → 发信到 `support@ideahubs.org`
- **面向 13 岁以上用户；未满 18 周岁请在监护人知情同意下使用**

### 10.4 用户协议要点（App 内，）
- 三、AI 生成服务与虚拟资产：报价在操作当场显示、按实际用量结算；生成可能因内容审核不通过而失败；**token 与创作收益仅限本应用内使用，不支持提现，不可兑换法定货币**
- 四、内容规范：不得制作/上传违法内容；上传素材（含照片与声音样本）须为你拥有权利或已取得授权
- 五、AI 生成内容标识：依据《人工智能生成合成内容标识办法》在成片画面添加持续显示的「AI 生成」标识，**任何人不得删除、篡改或隐匿**
- 七、内容管理：**发布即定稿**，已发布作品的成片内容不可修改；编辑页仅可改标题、封面、可见性等
- 八、免责：**AI 生成结果的质量与可用性不作保证**
- 九、未成年人：应在监护人同意与指导下使用；**涉及充值消费的须经监护人同意**

### 10.5 AIGC 标识（AIGC 内容须知，`:120-145`）
- **两处显式标识，都不可关闭 / 不得删除、遮挡或篡改**：
  1. **成片画面右下角**持续显示的「AI 生成」角标（合成导出时逐帧写入，`drawAigcBadge`）
  2. **作品周边**：首页信息流与作品详情页在作品旁显示「AI 生成」标识（`components/AigcBadge`）
-
- 标识组件不做成按钮（免得让人以为可以关掉），hover 文案「本内容由人工智能生成或合成」
- 本 app 里**每一条作品都是 AI 生成的**，`isAigcWork` 今天恒为真
- ⚠ **「元数据隐式标识 / aigc metadata」还没实现，文案里不许说**
- ⚠ 用户问到「AI 合成语音」badge：仓库里**没有单独的「AI 合成语音」标识组件**，只有统一的「AI 生成」标识 `[待确认]`（全 app 只有 `AigcBadge` 一处 AIGC 标识实现，见其文件头「全 app 唯一的一份」）

### 10.6 年龄与内容限制
- 隐私政策：面向 **13 岁以上**用户
- 用户协议第九条：未成年人应在监护人同意与指导下使用，充值须经监护人同意
- 素材授权：使用真人形象、声音须取得本人明确授权；由素材权利瑕疵引起的纠纷**由发布者自行承担**

---

## 11. 服务端对 App 的影响（限流 / 钱包 / 通知）

### 11.1 限流
- 超限统一返回 **429 `RATE_LIMIT_EXCEEDED`**，带 `Retry-After` 头与 `details.retryAfter`（秒）
- 计数后端：Redis 优先（跨进程），不可用时回退进程内 Map（配额会被稀释但不会整站不可用）
- **登录类双维度**：按账号严（15 分钟 10 次，`LOGIN_RATE_ACCOUNT_MAX`）、按 IP 松（60 秒 60 次，`LOGIN_RATE_IP_MAX`）
- 已登录端点按**账号**限流，不按 IP
- AI 端点默认 20 次/分钟（按用户）

关键限流值（客服判断「是不是被限流了」）：
| 端点 | 限流 | 出处 |
|---|---|---|
| 邮箱验证码 start | 10/分钟 | , `:10` |
| **手机验证码 start** | **5/分钟**（真发短信、真扣费） | ； |
| 手机验证码 verify | 20/分钟 |  |
| QQ 原生登录 | 20/分钟·IP |  |
| 方舟出图/出视频/对话 | 30/分钟 |  |
| 方舟轮询任务 | 90/分钟 |  |
| MiniMax 出片 / 轮询 | 30 / 90 每分钟 |  |
| TTS | 30/分钟 |  |
| 播放计数 | 60/分钟 |  |
| 点赞 / 收藏 / 评论赞 | 60/分钟 | , `:60`, `:90` |
| 发评论 | 20/分钟 |  |
| 发弹幕 / 删弹幕 | 30/分钟 | , `:128` |
| 举报 | 10/分钟 + 50/24小时 |  |
| 上传 | 20/分钟 |  |
| 模板视频上传 | 3/分钟 + 10/天 |  |
| 白模化 | 3 / 10 分钟 |  |
| 搜人 | 120/分钟 |  |
| 下单（充值/套餐） | 20/分钟 | , `:77`； |

### 11.2 充值订单
- 订单状态机：`created → paid → settled`，或 `closed` / `failed`
- `GET /api/pay/config` 返回 `payable`；**`payable=false` = 现在收不了钱，UI 必须说出来**
- **⚠ 现在一个真实支付渠道都没接**（`channels.js` 注册表是空的），下单能下但没人会把订单推进到 settled。这是**故意**的
- App 侧文案：`payable ? "订单已创建，请完成支付" : "服务端还没接入支付渠道，暂时无法充值"`
- 钱包路由 `/recharge` `/plan` 现在只下单，返回 **202**，余额不变

### 11.3 点数 / 悬赏 / 人格购买（官网产品线）
- 服务端有 `points`（虚拟点数）、Bounty、Persona 购买等模型
- 服务端明确写着：「★这是平台【虚拟点数】，不是真钱：无现金价值，不可提现/兑换，不接任何真实支付。」
- **App 完全不接这些接口**：`app/src/api/` 下无 points / bounty / persona 任何调用（grep 全仓无命中）。App 只有 token 钱包一套货币

### 11.4 其它服务端事实
- 通知类型 enum 见 （含官网侧的 LIKE/COMMENT/MESSAGE_REQUEST_* 等，App 只筛 6 类 BRANCH_*/ADMIN_NOTICE）
- `ADMIN_NOTICE` **不带 actorId**：以平台口径发出，不透给用户是哪个管理员
- 服务端 zod schema 默认 **strip 未声明字段**（客户端发了、201 了、读回来是空的，全程零报错）
- 用户协议同意留痕：`POST /api/me/accept-terms`，**服务端只存不判**，是合规留痕不是门禁

---

## 客服禁止承诺的事项

以下每条都有代码/文档依据，AI 客服**绝不可**向用户承诺：

### 钱与退款
1. **不承诺任何退款**。契约明写：任务**被受理之后**才失败（Seedance 跑完报 failed）**不退**，「刻意为之，不是遗漏」
2. **只有一种自动退回**：上游**未受理**（400 敏感词 / 429 / 5xx / 501）时原路退回 **addon**，且不退回 plan
3. **不承诺 token 可以提现或兑换现金**
4. **不承诺现在能充值/订阅成功**：一个真实支付渠道都没接，下单不会被推进到 settled
5. **不承诺付费作品能赚钱**：「付费解锁」已下架，服务端**没有任何解锁/分账端点**
6. **不承诺 App 内能查 token 流水**：`fetchWalletLedger` 在 App 里零调用方，App 内**根本没有流水页面**
7. **真人档（MiniMax）不许说「24 小时内可取回」，也不许说「已经没了」**：我们从没量过 MiniMax 的留存
8. **不承诺「重试一次不花钱」**：这条路上「重试」= 重新下一单 = 再花一次钱；只有「取回」不花钱

### 平台与分发
9. **没有 iOS 版**：下载页固定提示「iOS 版还没有，这是安卓安装包」
10. **不承诺「在应用商店能搜到」**：目前是侧载分发（官网 `ideahubs.org/download`），Google Play 上架另有清单尚未完成
11. **上架（play）渠道的包没有应用内更新**，设置页只会显示「由应用商店负责更新」
12. **不承诺「清缓存/重装能解决更新失败」**：同版本号发第二份内容会让老用户永久更新不了，出路是等新版本号

### 账号与数据
13. **不承诺能找回原密码**：密码 bcrypt 哈希保存，看不到明文
14. **不承诺 username 可以改**：注册时定下、之后不可改
15. **不承诺「注销后 X 天彻底删除数据」**：代码只做软删除，页面刻意不写这类没实现的话
16. **不承诺「注销后能自助恢复」**：恢复要发邮件到 `support@ideahubs.org` 由管理员操作
17. **不承诺草稿跨设备同步**：草稿只在这台设备的 IndexedDB，卸载/换设备就没
18. **不承诺人物卡的声音样本跨设备/随分享**：明确写着「声音不跨设备、不随分享」
19. **不承诺草稿能存超过 20 条**：超量按最旧的自动删除
20. **不承诺已发布作品能改内容**：发布即定稿，编辑页只改标题/分类/简介/封面/可见性
21. **不承诺分集（多 P）能增删改**：服务端 BranchVideo **压根没有 parts 字段**，那套按钮已删

### 功能范围
22. **App 里没有私信 / 好友申请 / 黑名单 / 拉黑功能**（那是官网产品线）
23. **App 里没有在线客服 / 内嵌工单 / 聊天窗**（全 app 无实现；唯一联系方式是邮箱 `support@ideahubs.org`）
24. **App 不接积分 / 悬赏 / 人格购买**（那些是官网的虚拟点数体系，与 App 的 token 钱包无关）
25. **不承诺人工客服响应时间 / 服务时段** `[待确认]` — 仓库中无任何 SLA、工作时间或响应承诺
26. **不承诺 AI 生成质量**：协议第八条明写「AI 生成结果的质量与可用性不作保证」

### 技术能力
27. **不许说段与段「无缝衔接」「严丝合缝」**：高清/电影级两档是软引导不是硬约束
28. **不许说圈选「一定按你圈的改」**：已从硬约束变成软引导
29. **不承诺可以上传真人照片出片**（除「真人」档外四档全部拒收，整发被拒不是降级）；合规出路是肖像授权拿 `asset://`
30. **不承诺白模模板出片在所有档位可用**：仅「电影级」开闸，其余三档整句拒「暂未开放白模模板出片」
31. **不承诺真人卡能用白模模板**：双向无解组合
32. **不承诺给图生 3D 的角色能加眨眼/口型/表情**：做不成，表情能力是资产属性
33. **不承诺「aigc 元数据隐式标识」已实现**：只有画面角标 + 界面标识两处显式标识
34. **不承诺 App 内有「AI 合成语音」独立徽标** `[待确认]` — 仓库只有统一的「AI 生成」标识组件
35. **不承诺服务器在中国内地**：服务器在**中国香港**（阿里云），数据库 MongoDB Atlas
36. **不承诺提示词想写什么就写什么**：含敏感词是整发 400 拒收，不是降级

---

## 附：转人工的判定建议（供实现 AI 客服时参考，非代码事实）

以下情形 AI 客服应直接转人工（建工单 + 通知管理员 + 邮件），不要自行处理：
- 涉及钱：要求退款、余额对不上、充值没到账、「取回」过期后要求补偿（尤其真人档「把任务号发给客服还有机会」那条）
- 涉及账号：注销后要求恢复、要求彻底删除数据、被封禁申诉、账号被盗
- 涉及内容处置：作品被下架申诉、举报结果异议、侵权投诉
- 用户明确要求人工，或连续两轮未解决
- AI 无法从本知识库找到依据的问题（不得编造）